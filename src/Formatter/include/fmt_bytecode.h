#ifndef FMT_BYTECODE_H
#define FMT_BYTECODE_H

#include <stddef.h>

/*
 * Format Output for Bytecode.
 */
void Formatter_bytecode(
    char* code,     /* [in] bytecode */
    size_t size     /* [in] bytecode size */
);

#endif
